# pnpm only runs dependency install scripts that are listed here as true.
allowBuilds:
  "@prisma/engines": true # downloads the Prisma schema engine used by migrations
  esbuild: true # platform binary check (used by tsx)
  prisma: true
  sharp: false
  unrs-resolver: false
